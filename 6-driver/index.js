const hasLicence = true;
const age = 24;
const isDrunk = false;

if (hasLicence && age > 18 && !isDrunk) {
  console.log("Пользователь может водить автомобиль");
} else {
  console.log("Пользователь не может водить автомобиль");
}
